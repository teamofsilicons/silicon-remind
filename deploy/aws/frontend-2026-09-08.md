# Frontend deployment — 2026-09-08

- Public URL: https://remind.teamofsilicons.com
- AWS account/region: `234951665042` / `us-east-1`.
- Existing standalone ARM64 instance: `i-0546693fac4a32d6d`, public IP `44.204.214.85`.
- ECR tag: `silicon-remind-production:frontend-20260908-01`.
- Deployed image: `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-remind-production@sha256:eeeec3c91b007948f06eceb2803bffc21bc4e149f17edb20f9703536b120b486`.
- Namecheap CNAME: `remind` → `backend.remind.teamofsilicons.com`, TTL 300,
  record ID `534095396`. Existing DNS records preserved.
- SSM installation command: `ef1ec664-9b9d-4b13-be27-18757713bc99` (Success).
- Restart verification command: `29c6f2cd-ba49-4295-bca4-dd5fa702580a` (Success).

The Node 24 production gateway serves the built SolidJS assets through Caddy
with automatically renewed trusted HTTPS. It connects to the existing public
Remind backend and hosted IAM. No load balancer was created or attached. API,
worker and Caddy containers remained running during frontend installation and
restart verification.

## Verified

- ARM64 Docker build passed the frontend gateway tests, TypeScript checks and
  optimized production build.
- Authoritative DNS, Cloudflare and Google public resolvers returned the new
  CNAME and backend IP.
- Trusted TLS and HTTP 200 for the HTML page and all three linked asset URLs.
- Fresh `/ui/session` returned an unauthenticated session summary.
- `/ui/api/health/ready` returned backend `status: ok`.
- Cross-origin auth-start POST was rejected with 403.
- Same-origin auth-start produced an IAM login URL with `tos>remind`, selected
  organization and the HTTPS Remind callback with correlation state.
- IAM login returned its HTML application with a browser-style Accept header.
- Restarting `remind-frontend.service` restored service, and normal DNS/HTTPS
  requests from EC2 returned the frontend and healthy backend response.
- Session key permissions were 0600 and session directory permissions 0700.
  Both are host-mounted and preserved when the frontend container is replaced.

The local workstation resolver retained an earlier negative DNS answer during
verification. Local HTTPS probes therefore used curl's `--resolve` with the
actual public hostname (certificate verification enabled); server-side probes
used normal DNS. A fresh authenticated browser round trip on the new public
hostname was not completed during this deployment. Earlier local browser and
workflow evidence is documented in [the frontend README](../../frontend/README.md)
and [browser E2E report](../../frontend/docs/browser-e2e-2026-09-06.md).
