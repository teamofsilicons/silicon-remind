# First-reminder fix deployment — 2026-09-08

Source commit `8912907` fixes first reminder creation for an IAM-authenticated
Silicon with no webhook subscriptions. No API contract or migration changed.

- Instance: `i-0546693fac4a32d6d`, account `234951665042`, region `us-east-1`.
- ECR tag: `silicon-remind-production:resume-8912907-aws`.
- Deployed API/worker image:
  `234951665042.dkr.ecr.us-east-1.amazonaws.com/silicon-remind-production@sha256:257c4f53ee2e507265fa9345fd7a302ada7f6d5b9b6c8587bba8e8d281a72dc6`.
- Previous runtime digest:
  `sha256:a105d3110d1f92d80fd53f22f264c6afa998a5e82f7267fdf367a5464552cab0`.
- Successful preflight command: `10d97f0f-35e6-4b3e-9533-6bc8b6e9e371`.
- Successful deployment command: `ae05739b-f42a-4f38-9114-41c0163dfd8f`.
- Final healthy-container check: `1259e37d-6d46-4130-b694-cd35b0c5e2e0`.

The initial base-image attempt failed because it omitted the AWS RDS CA bundle
referenced by the production database URLs. Automatic rollback restored the
previous runtime. Wrapping the base image with `deploy/aws/Dockerfile.runtime`
resolved this packaging error without weakening TLS verification. A temporary
API container using the real runtime configuration then passed readiness before
the second rollout. The standalone instructions now explicitly require this
wrapper and preflight.

The successful update gracefully stopped and replaced only the API and worker,
preserving their runtime configuration and container restrictions. Caddy and
the frontend were not restarted. Both image references in
`/usr/local/sbin/remind-start` were updated; the prior startup script is saved as
`/usr/local/sbin/remind-start.before-8912907`. Future infrastructure reprovisioning
must use the new wrapped image digest; CloudFormation was not changed by this
in-place update. No DNS, load balancer, database schema, or credential change
was needed.

## Public verification

- Both containers reported `healthy` on the new digest; `remind` and
  `remind-frontend` systemd units remained active.
- Backend `/health/ready` and frontend `/ui/api/health/ready` returned 200.
- Created a disposable public Remind sandbox linked to the dedicated QA IAM
  environment and authenticated a fresh Silicon through real IAM.
- Before the update, empty subscriptions followed by first reminder creation
  reproduced `409 silicon_unavailable`. After the update, creation returned 201
  for the same Silicon, still with no subscriptions.
- Schedule `01a080d4-afbf-74c3-84b0-a4c6ec13c2b8` fired at
  `2026-09-08T11:44:00Z`. Execution
  `01a080d5-0982-7e73-8832-9a7afe2fc9a9` was processed successfully at
  `11:44:00.422367Z`, with no failure or outbound webhook requests.
- The disposable public Remind and IAM environments were retired after the
  check. Production reminder data was not modified.

Local release evidence includes 127 passing Rust tests, strict Clippy,
formatting, an ARM64 image build, and real signed/unsigned two-receiver delivery.
See [manual acceptance](../../docs/MANUAL_ACCEPTANCE.md).
