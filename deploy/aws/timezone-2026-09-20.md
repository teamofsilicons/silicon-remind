# Explicit timezone deployment — 2026-09-20

Reminder creation now requires an explicit IANA timezone in the API, CLI, Rust
client, and web form. Missing, null, empty, or whitespace-only timezone values
return `422 timezone_required`; PATCH omission continues to retain the timezone.

## Deployed artifacts

The existing ARM64 host `i-0546693fac4a32d6d` in account `234951665042`, region
`us-east-1`, continues to run the API, worker, frontend, Caddy, and documentation.
All image digests below belong to ECR repository `silicon-remind-production`.

| Component | Source revision | Image digest |
| --- | --- | --- |
| API and worker | `b19cec0d2ff135e6bd018fac9d03c1e1767c649c` | `sha256:c4036287b448ec9fba590ea65dab924c267e0ca469e32dd9967ee950dafba200` |
| Frontend | `e6a9fd94d13dc632adefe1c09f5921b74b172608` (`v0.3.0`) | `sha256:941f49c79a3b89ef21ef93d1b1dc467704c9a8634ebeeabefa6bae4ff36d5e99` |
| Documentation artifact | `7f450728aeb1e1cee82bcd314a5a700ea1a10065` | `sha256:2d50863bb64c2147b1077b73a55e291f6f8a64bb3407a29622177bd8f179fbfa` |

The backend was cross-built with `cargo zigbuild --release --locked` for
`aarch64-unknown-linux-gnu.2.28`. The three binaries were added to the pinned
existing ARM64 runtime, retaining its RDS CA bundle. Frontend tests and its
production build passed; documentation checked 22 pages and 620 links.
Artifacts were published through ECR APIs without running Docker locally.
The backend package still reports version `0.1.0`; the image revision and digest
identify this deployment precisely.

## Migration and lifecycle configuration

Preflight found an existing testing control migration numbered `0004_imported_environment_names`.
Its SQL checksum exactly matched the file that had been renamed to `0005` in
source. Commit `89c67cc` restores that original filename and places the new
Honeycomb lifecycle migration at `0005`. Both SQL files are byte-identical to
their prior contents. No migration ledger or checksum was rewritten.

The existing migration owner applied the new control migration successfully;
the runtime database credential retains its existing permissions. The live
ledger preserves migration 4 checksum
`85e3a74bb6dd53606a7f8a1638d31251842e8043a65a8f5ca5ed6cebf9851b33983ca38177ba0c4d90de8f3a79d8ff23`
and adds migration 5 checksum
`5195a76ac3578672f157cf6cc5351592ec25add870a9d260135714d086ccb471af44eb0bca57477825a1b23cc3ae1435`.
Fresh control-database and historical-0004 upgrade checks passed, including
idempotent reruns. The temporary encrypted migration parameter and host env file
were removed after use.

`silicon-remind/runtime-production` now contains the dedicated
`REMIND_HONEYCOMB_SERVICE_TOKEN` and
`REMIND_HONEYCOMB_BASE_URL=https://backend.honeycomb.teamofsilicons.com`.
The matching dedicated token and Remind participant were configured in Honeycomb.
Only those two fields were added to the host runtime environment; every existing
runtime value, encryption key, frontend session key, and data mount was retained.
Caddy exposes `/internal/honeycomb/organizations/*` to Remind's service-token
authentication. Other internal paths remain unavailable publicly.

## Sandbox authentication repair

Live acceptance found that the IAM SDK testing selector retained the previously
configured production Authorization credential. Commit `b19cec0` explicitly uses
the test application credential for both Authorization and X-Testing-Application
during discovery and subsequent IAM requests. A wiremock regression requires
both headers and verifies the returned client remains bound to testing; all three
IAM unit tests and strict Clippy passed. Production authentication is unchanged.
The complete CI run for that revision passed:
https://github.com/teamofsilicons/silicon-remind/actions/runs/35522443566.

## Rollout and checks

A temporary API container passed readiness with the actual runtime configuration
before the API and worker were replaced. Both production containers then reported
`healthy` and the expected revision and digest. Their startup script now pins the
new image. Caddy was validated and reloaded. Existing telemetry mounts, frontend
session storage, and database configuration remain in place.

- Backend `/health/ready` returned 200.
- The protected Honeycomb lifecycle route returned 401 without a service token;
  an unrelated `/internal/` route returned 404.
- The public frontend serves `index-CYXJgHry.js`, containing the mandatory IANA
  timezone guidance.
- Documentation `/`, `/api/`, `/openapi.yaml`, and `/release-0.3.0/` returned 200.
  The API contract and release page contain `timezone_required`.
- Native PostgreSQL/API/CLI/worker end-to-end verification passed again after
  the migration fix, including actual UTC and Asia/Kolkata webhook delivery.
- Authenticated production probes for both recurring and one-time creation,
  each with omitted, null, empty, and whitespace-only timezone values, returned
  `422 timezone_required` with IANA/JSON-field guidance in all eight cases.
  A subsequent listing found no schedules created by those rejection probes.
- Honeycomb retried the previously pending Remind import successfully and
  reported all participants ready.
- Live isolated acceptance passed all eight missing/blank timezone rejection
  cases without creating records, and rejected an invalid IANA identifier.
  For recurring and one-time reminders in both UTC and Asia/Kolkata, the API
  verified local 09:00 scheduling, idempotent creation replay, reads, and
  text-only edits retaining the timezone. The published registry CLI `0.3.0`
  also created and read all four combinations against the deployed backend.
- Two live one-time reminders, one in each timezone, executed at the same
  intended instant, `2026-09-20T16:33:00Z`, and became completed/archived.
  These used the real worker with no outbound subscriptions. Actual webhook
  delivery was verified separately in the native local end-to-end run.
- The live acceptance run required seven bounded retries. IAM logs confirmed
  testing-context rate limiting (`429`), surfaced by Remind as dependency
  unavailable (`503`). Retried mutations reused their idempotency keys. This
  is an upstream throttling limitation; no IAM rate-limit policy was changed.
- Disposable environment `1cd27311-b38e-4e3c-a99d-51eed99206d5` was deleted
  using lifecycle operation `7544fdce-09f0-42cc-a234-17e71286ad3e`, revision 3.
  Honeycomb, IAM, and Remind all completed the operation, with no pending work.
  The previously authenticated test session then received `401`. Local test
  credentials were removed after copying sanitized results.

## Publication

Both `silicon-remind-client` and `silicon-remind-cli` version `0.3.0` are published
on crates.io. All six native platform builds passed their CLI tests. The GitHub
release is https://github.com/teamofsilicons/silicon-remind/releases/tag/v0.3.0.
Its seven-file archive has SHA-256
`9a3cc28b131be18fbac95223d49ad249a12cc7b6f3517f3fd8f0574377deb4f9`.
Honeycomb accepted the same archive as release
`708f11dd-e456-49ef-9dc2-3788a8737a30`; a fresh anonymous default-latest install
resolved `0.3.0` and passed the missing/value-less/blank timezone checks.
Maharaj's installed `tos>remind` package was updated and reports `remind 0.3.0`.

Sanitized local acceptance evidence is retained under
`target/release-0.3.0/`, including `live-api-results.json`,
`live-sandbox-results.json`, `sandbox-cleanup.json`, and anonymous-install
results. These local artifacts are not required to build the release.

## Operational references

Documentation is selected by a relative symlink at
`/var/lib/remind-docs/current`, pointing to
`releases/7f450728aeb1e1cee82bcd314a5a700ea1a10065`; relative paths also resolve
inside the docs container's `/srv` mount.

Deployment evidence in SSM:

- Migration: `cd142258-1e2b-44c5-9bba-01c9b08bb374`.
- Candidate readiness: `5c23f880-95d7-407f-8e62-960d25476f5a`.
- API/worker cutover: `2f4802dc-2c6b-41e3-a848-4fe6318e86c7`.
- Final health/configuration check: `d88fac84-dc28-4da6-9b6b-2d83496fbe07`.
- Final documentation selection: `062a250c-4074-4213-b769-c3f931dba6e5`.
- IAM-fix candidate readiness: `10853312-71e6-467c-a4d5-c8ddb28f9df9`.
- IAM-fix API/worker cutover: `53e92bc7-f6e9-4c84-b58e-e6871ec51a74`.
- IAM-fix delayed health check: `1a04cb6e-c79f-4aa2-b181-be0d91e29192`.
- IAM throttle diagnosis: `1311659e-ffaa-49d5-a792-3cb1cbca5789`.

Prior API/worker digest
`sha256:04437bd5572492bba9ed6c54b4a099fa3a336484f8d78449de883a45c4b9709b`
and frontend digest
`sha256:89e7dcc0d067cd24f25df7b319bf8be53b7412c98cb53608bf6567f70fbc0c3d`
remain available. Host configuration backups use the `.before-v030` suffix.
This was an in-place rollout; future CloudFormation reprovisioning must use the
new backend digest and rerun the separate frontend/docs installation.
