# Remind documentation site

Run `npm ci`, `npm run build`, and `npm run check`. Publish `dist/` with directory-index serving at https://docs.remind.teamofsilicons.com. The build renders the repository manuals, OpenAPI, search, installer, and public source bundle. No application credentials are required. See `deploy/aws/deploy-docs.py` for the existing Remind server hosting path.
